package com.kiselev.instagram.model.min.mapper;

import com.kiselev.instagram.model.InstagramProfile;
import com.kiselev.instagram.model.min.InstagramProfileMin;

public class InstagramMapperMin {

    public InstagramProfileMin profile(InstagramProfile profile) {
        InstagramProfileMin profileMin = new InstagramProfileMin();

        // TODO: Mapping only business fields

        return profileMin;
    }
}
